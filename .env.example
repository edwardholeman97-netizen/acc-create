# CDS Account – copy to .env and set values. Do NOT commit .env to version control.
# Production: set these in the server environment (Apache SetEnv, PHP-FPM env, systemd, etc.) and do not use a .env file.

# CSE API (required) – get these from CSE; without them dropdowns (brokers, banks, etc.) will fail with auth error
CSE_API_BASE_URL=https://uat-cseapi.cse.lk
CSE_API_USERNAME=your_cse_username
CSE_API_PASSWORD=your_cse_password

# Environment: production | staging | local
APP_ENV=local
APP_DEBUG=1

# CORS: in production set to your frontend origin, e.g. https://cds.yourdomain.com
CORS_ALLOW_ORIGIN=*

# Optional: custom path for storage (token cache, logs). Default: project_dir/storage
# CSE_STORAGE_PATH=/var/lib/cds-acc/storage
# CSE_TOKEN_VALID_SECONDS=840
